// will contain configuration for information like the database credentials and
// the rows we want to show per page when we paginate results.

const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: 'password',
        database: 'trendsDB'
    },
};

module.exports = config;